<%= form_with(model: prompt_category, local: true) do |form| %>
  <% if prompt_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prompt_category.errors.count, "error") %> prohibited this prompt_category from being saved:</h2>

      <ul>
        <% prompt_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :label %>
    <%= form.text_area :label %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_area :color %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
